import random as rnd

opciones = ['Maria','Max','Victor','Alondra']

#eleccion = rnd = random.choice(opciones)

#eleccion = rnd.randit(1,5000) #devuleve un valor de 1 y maximo 5000

#eleccion = rnd.random() #devuelve valor entre 0 y 1

#eleccion = rnd.uniform(1.5,7.3)

print(eleccion) # type: ignore 